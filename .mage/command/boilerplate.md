---
description: Manage boilerplate profiles. Usage: /boilerplate [list|info|use <name>|add <name> <git-url>]
---

Call `mage_boilerplate_manage` with the arguments "$ARGUMENTS". If no argument is given, use action="list". For "add <name> <url>", pass action="add", name, and url separately.
