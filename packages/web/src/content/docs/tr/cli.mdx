---
title: CLI
description: mage CLI seçenekleri ve komutları.
---

import { Tabs, TabItem } from "@astrojs/starlight/components"

mage CLI herhangi bir argüman olmadan çalıştırıldığında varsayılan olarak [TUI](/docs/tui)'yi başlatır.

```bash
opencode
```

Ancak bu sayfada belgelendiği gibi komutları da kabul eder. Bu, mage ile programlı olarak etkileşim kurmanıza olanak tanır.

```bash
mage run "Explain how closures work in JavaScript"
```

---

### tui

mage terminal kullanıcı arayüzünü başlatır.

```bash
mage [project]
```

#### Bayraklar

| Bayrak                                   | Kısa | Açıklama                                                                    |
| ---------------------------------------- | ---- | --------------------------------------------------------------------------- |
| <nobr><code>{"--continue"}</code></nobr> | `-c` | Son oturuma devam et                                                        |
| <nobr><code>{"--session"}</code></nobr>  | `-s` | Devam edecek oturum kimliği                                                 |
| <nobr><code>{"--fork"}</code></nobr>     |      | Devam ederken oturumu fork'lar (`--continue` veya `--session` ile kullanın) |
| <nobr><code>{"--prompt"}</code></nobr>   |      | Kullanılacak prompt                                                         |
| <nobr><code>{"--model"}</code></nobr>    | `-m` | provider/model biçiminde kullanılacak model                                 |
| <nobr><code>{"--agent"}</code></nobr>    |      | Kullanılacak agent                                                          |
| <nobr><code>{"--port"}</code></nobr>     |      | Dinlenecek port                                                             |
| <nobr><code>{"--hostname"}</code></nobr> |      | Dinlenecek host adı                                                         |

---

## Komutlar

mage CLI ayrıca aşağıdaki komutlara da sahiptir.

---

### agent

mage için agent'ları yönetin.

```bash
mage agent [command]
```

---

### attach

`serve` veya `web` komutlarıyla başlatılan, halihazırda çalışan bir mage arka uç sunucusuna bir terminal ekleyin.

```bash
mage attach [url]
```

Bu, TUI öğesinin uzak bir mage arka ucuyla kullanılmasına olanak tanır. Örneğin:

```bash
# Start the backend server for web/mobile access
mage web --port 4096 --hostname 0.0.0.0

# In another terminal, attach the TUI to the running backend
mage attach http://10.20.30.40:4096
```

#### Bayraklar

| Bayrak                                   | Kısa | Tanım                                                                                         |
| ---------------------------------------- | ---- | --------------------------------------------------------------------------------------------- |
| <nobr><code>{"--dir"}</code></nobr>      |      | TUI'yi başlatmak için çalışma dizini                                                          |
| <nobr><code>{"--continue"}</code></nobr> | `-c` | Son oturuma devam et                                                                          |
| <nobr><code>{"--session"}</code></nobr>  | `-s` | Devam edecek oturum açma bilgileri                                                            |
| <nobr><code>{"--fork"}</code></nobr>     |      | Devam ederken oturumu çatalla (`--continue` veya `--session` ile kullanın)                    |
| <nobr><code>{"--password"}</code></nobr> | `-p` | Temel kimlik doğrulama parolası (varsayılan: `MAGE_SERVER_PASSWORD`)                      |
| <nobr><code>{"--username"}</code></nobr> | `-u` | Temel kimlik doğrulama kullanıcı adı (varsayılan: `MAGE_SERVER_USERNAME` veya `mage`) |

---

#### create

Özel yapılandırmaya sahip yeni bir agent oluşturun.

```bash
mage agent create
```

Bu komut, özel sistem istemi ve araç yapılandırmasıyla yeni bir agent oluşturma konusunda size yol gösterecektir.

---

#### list

Mevcut tüm agent'ları listeleyin.

```bash
mage agent list
```

---

### auth

Sağlayıcılar için kimlik bilgilerini ve oturum açmayı yönetme komutu.

```bash
mage auth [command]
```

---

#### login

opencode, [Models.dev](https://models.dev) adresindeki sağlayıcı listesi tarafından desteklenmektedir, dolayısıyla kullanmak istediğiniz herhangi bir sağlayıcı için API anahtarlarını ayarlamak üzere `mage auth login` kullanabilirsiniz. Bu `~/.local/share/opencode/auth.json`'de saklanıyor.

```bash
mage auth login
```

mage başlatıldığında sağlayıcıları kimlik bilgileri dosyasından yükler. Ayrıca ortam değişkenlerindeki veya projenizdeki `.env` dosyasındaki anahtarları da kullanır.

---

#### list

Kimlik bilgileri dosyasında depolanan tüm kimliği doğrulanmış sağlayıcıları listeler.

```bash
mage auth list
```

Veya kısa versiyonu.

```bash
mage auth ls
```

---

#### logout

Bir sağlayıcıyı kimlik bilgileri dosyasından temizleyerek oturumunuzu kapatır.

```bash
mage auth logout
```

---

### github

Depo otomasyonu için GitHub aracısını yönetin.

```bash
mage github [command]
```

---

#### install

GitHub aracısını deponuza yükleyin.

```bash
mage github install
```

Bu komut gerekli GitHub Actions workflow'unu kurar ve yapılandırma adımlarında size rehberlik eder. [Daha fazla bilgi](/docs/github).

---

#### run

GitHub aracısını çalıştırın. Bu genellikle GitHub Eylemlerinde kullanılır.

```bash
mage github run
```

##### Bayraklar

| Bayrak                                | Açıklama                                    |
| ------------------------------------- | ------------------------------------------- |
| <nobr><code>{"--event"}</code></nobr> | Aracıyı çalıştırmak için GitHub sahte olayı |
| <nobr><code>{"--token"}</code></nobr> | GitHub personal access token                |

---

### mcp

Model Bağlam Protokolü sunucularını yönetin.

```bash
mage mcp [command]
```

---

#### add

Yapılandırmanıza bir MCP sunucusu ekleyin.

```bash
mage mcp add
```

Bu komut, yerel veya uzak bir MCP sunucusu ekleme konusunda size yol gösterecektir.

---

#### list

Yapılandırılmış tüm MCP sunucularını ve bağlantı durumlarını listeleyin.

```bash
mage mcp list
```

Veya kısa versiyonunu kullanın.

```bash
mage mcp ls
```

---

#### auth

OAuth'un etkin olduğu bir MCP sunucusuyla kimlik doğrulaması yapın.

```bash
mage mcp auth [name]
```

Sunucu adı belirtmezseniz mevcut OAuth özellikli sunucular arasından seçim yapmanız istenir.

Ayrıca OAuth özellikli sunucuları ve bunların kimlik doğrulama durumlarını da listeleyebilirsiniz.

```bash
mage mcp auth list
```

Veya kısa versiyonunu kullanın.

```bash
mage mcp auth ls
```

---

#### logout

MCP sunucusu için OAuth kimlik bilgilerini kaldırın.

```bash
mage mcp logout [name]
```

---

#### debug

MCP sunucusu için OAuth bağlantı sorunlarının hatalarını ayıklayın.

```bash
mage mcp debug <name>
```

---

### models

Yapılandırılmış sağlayıcıların tüm mevcut modellerini listeleyin.

```bash
mage models [provider]
```

Bu komut, yapılandırdığınız sağlayıcılardaki modelleri `provider/model` formatında listeler.

Bu, [config dosyanızda](/docs/config/) kullanmanız gereken tam model adını bulmak için kullanışlıdır.

Modelleri bu sağlayıcıya göre filtrelemek için isteğe bağlı olarak bir sağlayıcı kimliğini iletebilirsiniz.

```bash
mage models anthropic
```

#### Bayraklar

| Bayrak                                  | Tanım                                                                       |
| --------------------------------------- | --------------------------------------------------------------------------- |
| <nobr><code>{"--refresh"}</code></nobr> | Modeller.dev'den model önbelleğini yenileyin                                |
| <nobr><code>{"--verbose"}</code></nobr> | Daha ayrıntılı model çıktısı kullanın (maliyetler gibi meta veriler içerir) |

Önbelleğe alınan model listesini güncellemek için `--refresh` bayrağını kullanın. Bu, bir sağlayıcıya yeni modeller eklendiğinde ve bunları opencode'da görmek istediğinizde kullanışlıdır.

```bash
mage models --refresh
```

---

### run

Doğrudan bir istem ileterek opencode'u etkileşimli olmayan modda çalıştırın.

```bash
mage run [message..]
```

Bu, komut dosyası oluşturma, otomasyon veya TUI'un tamamını başlatmadan hızlı bir yanıt istediğinizde kullanışlıdır. Örneğin.

```bash "opencode run"
mage run Explain the use of context in Go
```

Ayrıca, her çalıştırmada MCP sunucusunun soğuk önyükleme sürelerini önlemek için çalışan bir `mage serve` örneğine de ekleyebilirsiniz:

```bash
# Start a headless server in one terminal
mage serve

# In another terminal, run commands that attach to it
mage run --attach http://localhost:4096 "Explain async/await in JavaScript"
```

#### Bayraklar

| Bayrak                                   | Kısa | Açıklama                                                                                      |
| ---------------------------------------- | ---- | --------------------------------------------------------------------------------------------- |
| <nobr><code>{"--command"}</code></nobr>  |      | Çalıştırılacak komut, args için mesajı kullanın                                               |
| <nobr><code>{"--continue"}</code></nobr> | `-c` | Son oturuma devam et                                                                          |
| <nobr><code>{"--session"}</code></nobr>  | `-s` | Devam edecek oturum kimliği                                                                   |
| <nobr><code>{"--fork"}</code></nobr>     |      | Devam ederken oturumu fork'lar (`--continue` veya `--session` ile kullanın)                   |
| <nobr><code>{"--share"}</code></nobr>    |      | Oturumu paylaşın                                                                              |
| <nobr><code>{"--model"}</code></nobr>    | `-m` | provider/model biçiminde kullanılacak model                                                   |
| <nobr><code>{"--agent"}</code></nobr>    |      | Kullanılacak temsilci                                                                         |
| <nobr><code>{"--file"}</code></nobr>     | `-f` | Mesaja eklenecek dosya(lar)                                                                   |
| <nobr><code>{"--format"}</code></nobr>   |      | Biçim: varsayılan (biçimlendirilmiş) veya json (ham JSON olayları)                            |
| <nobr><code>{"--title"}</code></nobr>    |      | Oturumun başlığı (değer sağlanmazsa kısaltılmış bilgi istemi kullanılır)                      |
| <nobr><code>{"--attach"}</code></nobr>   |      | Çalışan bir mage sunucusuna ekleyin (ör. http://localhost:4096)                           |
| <nobr><code>{"--password"}</code></nobr> | `-p` | Temel kimlik doğrulama parolası (varsayılan: `MAGE_SERVER_PASSWORD`)                      |
| <nobr><code>{"--username"}</code></nobr> | `-u` | Temel kimlik doğrulama kullanıcı adı (varsayılan: `MAGE_SERVER_USERNAME` veya `mage`) |
| <nobr><code>{"--dir"}</code></nobr>      |      | Çalıştırılacak dizin veya bağlanırken uzak sunucudaki yol                                     |
| <nobr><code>{"--variant"}</code></nobr>  |      | Model varyantı (sağlayıcıya özgü muhakeme düzeyi)                                             |
| <nobr><code>{"--thinking"}</code></nobr> |      | Düşünme bloklarını göster                                                                     |
| <nobr><code>{"--port"}</code></nobr>     |      | Yerel sunucunun bağlantı noktası (varsayılan olarak rastgele bağlantı noktasıdır)             |

---

### serve

API erişimi için headless bir mage sunucusu başlatır. Tam HTTP arayüzü için [server docs](/docs/server) sayfasına bakın.

```bash
mage serve
```

Bu, TUI arayüzü olmadan mage işlevselliğine API erişimi sağlayan bir HTTP sunucusunu başlatır. HTTP temel kimlik doğrulamasını etkinleştirmek için `MAGE_SERVER_PASSWORD` öğesini ayarlayın (kullanıcı adı varsayılan olarak `mage` şeklindedir).

#### Bayraklar

| Bayrak                                   | Tanım                                      |
| ---------------------------------------- | ------------------------------------------ |
| <nobr><code>{"--port"}</code></nobr>     | Dinlenecek bağlantı noktası                |
| <nobr><code>{"--hostname"}</code></nobr> | Dinlenecek ana bilgisayar adı              |
| <nobr><code>{"--mdns"}</code></nobr>     | mDNS bulmayı etkinleştir                   |
| <nobr><code>{"--cors"}</code></nobr>     | CORS'a izin verecek ek tarayıcı kaynakları |

---

### session

mage oturumlarını yönetin.

```bash
mage session [command]
```

---

#### list

Tüm mage oturumlarını listeleyin.

```bash
mage session list
```

##### Bayraklar

| Bayrak                                    | Kısa | Tanım                                  |
| ----------------------------------------- | ---- | -------------------------------------- |
| <nobr><code>{"--max-count"}</code></nobr> | `-n` | En son N oturumla sınırla              |
| <nobr><code>{"--format"}</code></nobr>    |      | Çıkış formatı: tablo veya json (tablo) |

---

### stats

mage oturumlarınız için belirteç kullanımı ve maliyet istatistiklerini gösterin.

```bash
mage stats
```

#### Bayraklar

| Bayrak                                  | Açıklama                                                                                                    |
| --------------------------------------- | ----------------------------------------------------------------------------------------------------------- |
| <nobr><code>{"--days"}</code></nobr>    | Son N güne ait istatistikleri göster (tüm zamanlar)                                                         |
| <nobr><code>{"--tools"}</code></nobr>   | Gösterilecek araç sayısı (tümü)                                                                             |
| <nobr><code>{"--models"}</code></nobr>  | Model kullanım dökümünü göster (varsayılan olarak gizlidir). En üstteki N'yi göstermek için bir sayı iletin |
| <nobr><code>{"--project"}</code></nobr> | Projeye göre filtrele (tüm projeler, boş değer: mevcut proje)                                               |

---

### export

Oturum verilerini JSON olarak dışa aktarın.

```bash
mage export [sessionID]
```

Bir oturum kimliği sağlamazsanız mevcut oturumlar arasından seçim yapmanız istenir.

---

### import

Bir JSON dosyasından veya mage paylaşımından URL oturum verilerini içe aktarın.

```bash
mage import <file>
```

Yerel bir dosyadan veya mage paylaşımından (URL) içe aktarabilirsiniz.

```bash
mage import session.json
mage import https://opncd.ai/s/abc123
```

---

### web

Web arayüzüyle başsız bir mage sunucusu başlatın.

```bash
mage web
```

Bu, bir HTTP sunucusunu başlatır ve bir web arayüzü aracılığıyla opencode'a erişmek için bir web tarayıcısı açar. HTTP temel kimlik doğrulamasını etkinleştirmek için `MAGE_SERVER_PASSWORD` öğesini ayarlayın (kullanıcı adı varsayılan olarak `mage` şeklindedir).

#### Bayraklar

| Bayrak                                   | Tanım                                      |
| ---------------------------------------- | ------------------------------------------ |
| <nobr><code>{"--port"}</code></nobr>     | Dinlenecek bağlantı noktası                |
| <nobr><code>{"--hostname"}</code></nobr> | Dinlenecek ana bilgisayar adı              |
| <nobr><code>{"--mdns"}</code></nobr>     | mDNS bulmayı etkinleştir                   |
| <nobr><code>{"--cors"}</code></nobr>     | CORS'a izin verecek ek tarayıcı kaynakları |

---

### acp

Bir ACP (Ajan İstemci Protokolü) sunucusu başlatın.

```bash
mage acp
```

Bu komut, nd-JSON kullanarak stdin/stdout aracılığıyla iletişim kuran bir ACP sunucusunu başlatır.

#### Bayraklar

| Bayrak                                   | Açıklama            |
| ---------------------------------------- | ------------------- |
| <nobr><code>{"--cwd"}</code></nobr>      | Çalışma dizini      |
| <nobr><code>{"--port"}</code></nobr>     | Dinlenecek port     |
| <nobr><code>{"--hostname"}</code></nobr> | Dinlenecek host adı |

---

### uninstall

opencode'u kaldırın ve ilgili tüm dosyaları kaldırın.

```bash
mage uninstall
```

#### Bayraklar

| Bayrak                                      | Kısa | Tanım                                           |
| ------------------------------------------- | ---- | ----------------------------------------------- |
| <nobr><code>{"--keep-config"}</code></nobr> | `-c` | Yapılandırma dosyalarını sakla                  |
| <nobr><code>{"--keep-data"}</code></nobr>   | `-d` | Oturum verilerini ve anlık görüntüleri saklayın |
| <nobr><code>{"--dry-run"}</code></nobr>     |      | Nelerin kaldırılmadan kaldırılacağı göster      |
| <nobr><code>{"--force"}</code></nobr>       | `-f` | Onay istemlerini atla                           |

---

### upgrade

opencode'u en son sürüme veya belirli bir sürüme günceller.

```bash
mage upgrade [target]
```

En son sürüme yükseltmek için.

```bash
mage upgrade
```

Belirli bir sürüme yükseltmek için:

```bash
mage upgrade v0.1.48
```

#### Bayraklar

| Bayrak                                 | Kısa | Açıklama                                               |
| -------------------------------------- | ---- | ------------------------------------------------------ |
| <nobr><code>{"--method"}</code></nobr> | `-m` | Kullanılan kurulum yöntemi: curl, npm, pnpm, bun, brew |

---

## Global bayraklar

mage CLI aşağıdaki global bayrakları destekler.

| Bayrak                                     | Kısa | Tanım                                    |
| ------------------------------------------ | ---- | ---------------------------------------- |
| <nobr><code>{"--help"}</code></nobr>       | `-h` | Yardımı görüntüle                        |
| <nobr><code>{"--version"}</code></nobr>    | `-v` | Sürüm numarasını yazdır                  |
| <nobr><code>{"--print-logs"}</code></nobr> |      | Günlükleri stderr'e yazdır               |
| <nobr><code>{"--log-level"}</code></nobr>  |      | Günlük düzeyi (DEBUG, INFO, WARN, ERROR) |

---

## Ortam değişkenleri

mage ortam değişkenleri kullanılarak yapılandırılabilir.

| Değişken                              | Tip     | Açıklama                                                                    |
| ------------------------------------- | ------- | --------------------------------------------------------------------------- |
| `MAGE_AUTO_SHARE`                 | boolean | Oturumları otomatik olarak paylaş                                           |
| `MAGE_GIT_BASH_PATH`              | string  | Windows'ta yürütülebilir Git Bash'in Yolu                                   |
| `MAGE_CONFIG`                     | string  | Yapılandırma dosyasının yolu                                                |
| `MAGE_TUI_CONFIG`                 | string  | TUI yapılandırma dosyasının yolu                                            |
| `MAGE_CONFIG_DIR`                 | string  | Yapılandırma dizinine giden yol                                             |
| `MAGE_CONFIG_CONTENT`             | string  | Satır içi JSON config içeriği                                               |
| `MAGE_DISABLE_AUTOUPDATE`         | boolean | Otomatik güncelleme kontrollerini devre dışı bırak                          |
| `MAGE_DISABLE_PRUNE`              | boolean | Eski verilerin temizlenmesini devre dışı bırak                              |
| `MAGE_DISABLE_TERMINAL_TITLE`     | boolean | Otomatik terminal başlığı güncellemelerini devre dışı bırakın               |
| `MAGE_PERMISSION`                 | string  | Satır içi JSON izin config'i                                                |
| `MAGE_DISABLE_DEFAULT_PLUGINS`    | boolean | Varsayılan eklentileri devre dışı bırakın                                   |
| `MAGE_DISABLE_LSP_DOWNLOAD`       | boolean | Otomatik LSP sunucu indirmelerini devre dışı bırakın                        |
| `MAGE_ENABLE_EXPERIMENTAL_MODELS` | boolean | Deneysel modelleri etkinleştir                                              |
| `MAGE_DISABLE_AUTOCOMPACT`        | boolean | Otomatik context sıkıştırmayı devre dışı bırak                              |
| `MAGE_DISABLE_CLAUDE_CODE`        | boolean | `.claude`'den okumayı devre dışı bırak (istem + beceriler)                  |
| `MAGE_DISABLE_CLAUDE_CODE_PROMPT` | boolean | `~/.claude/CLAUDE.md` dosyasını okumayı devre dışı bırak                    |
| `MAGE_DISABLE_CLAUDE_CODE_SKILLS` | boolean | `.claude/skills` yüklemesini devre dışı bırak                               |
| `MAGE_DISABLE_MODELS_FETCH`       | boolean | Uzak kaynaklardan model getirmeyi devre dışı bırakın                        |
| `MAGE_FAKE_VCS`                   | string  | Test amaçlı sahte VCS sağlayıcısı                                           |
| `MAGE_CLIENT`                     | string  | Client kimliği (varsayılan: `cli`)                                          |
| `MAGE_ENABLE_EXA`                 | boolean | Exa web arama araçlarını etkinleştir                                        |
| `MAGE_SERVER_PASSWORD`            | string  | `serve`/`web` için temel kimlik doğrulamayı etkinleştirin                   |
| `MAGE_SERVER_USERNAME`            | string  | Temel kimlik doğrulama kullanıcı adını geçersiz kıl (varsayılan `mage`) |
| `MAGE_MODELS_URL`                 | string  | Model yapılandırmasını almak için özel URL                                  |

---

### Deneysel

Bu ortam değişkenleri değişebilecek veya kaldırılabilecek deneysel özellikleri etkinleştirir.

| Değişken                                        | Tip     | Tanım                                                   |
| ----------------------------------------------- | ------- | ------------------------------------------------------- |
| `MAGE_EXPERIMENTAL`                         | boolean | Şemsiye bayrağa bağlı deneysel özellikleri etkinleştir  |
| `MAGE_EXPERIMENTAL_ICON_DISCOVERY`          | boolean | Simge bulmayı etkinleştir                               |
| `MAGE_EXPERIMENTAL_DISABLE_COPY_ON_SELECT`  | boolean | TUI'da seçim yapıldığında kopyalamayı devre dışı bırak  |
| `MAGE_EXPERIMENTAL_BASH_DEFAULT_TIMEOUT_MS` | number  | MS cinsinden bash komutları için varsayılan zaman aşımı |
| `MAGE_EXPERIMENTAL_OUTPUT_TOKEN_MAX`        | number  | LLM yanıtları için maksimum çıktı belirteçleri          |
| `MAGE_EXPERIMENTAL_FILEWATCHER`             | boolean | Tüm dizin için dosya izleyiciyi etkinleştir             |
| `MAGE_EXPERIMENTAL_OXFMT`                   | boolean | Oxfmt biçimlendiriciyi etkinleştir                      |
| `MAGE_EXPERIMENTAL_LSP_TOOL`                | boolean | Deneysel LSP aracını etkinleştir                        |
| `MAGE_EXPERIMENTAL_DISABLE_FILEWATCHER`     | boolean | Dosya izleyiciyi devre dışı bırak                       |
| `MAGE_EXPERIMENTAL_EXA`                     | boolean | Deneysel Exa özelliklerini etkinleştirin                |
| `MAGE_EXPERIMENTAL_LSP_TY`                  | boolean | python dosyaları için TY LSP'yi etkinleştir             |
| `MAGE_EXPERIMENTAL_PLAN_MODE`               | boolean | Plan modunu etkinleştir                                 |
| `MAGE_EXPERIMENTAL_BACKGROUND_SUBAGENTS`    | boolean | Arka plan alt ajan görevlerini etkinleştir              |
| `MAGE_EXPERIMENTAL_EVENT_SYSTEM`            | boolean | Deneysel olay sistemini etkinleştir                     |
| `MAGE_EXPERIMENTAL_NATIVE_LLM`              | boolean | Yerel LLM istek yolunu etkinleştir                      |
| `MAGE_EXPERIMENTAL_PARALLEL`                | boolean | Paralel web araması yürütmesini etkinleştir             |
| `MAGE_EXPERIMENTAL_SCOUT`                   | boolean | Scout alt ajanını etkinleştir                           |
| `MAGE_EXPERIMENTAL_WORKSPACES`              | boolean | Çalışma alanı desteğini etkinleştir                     |
