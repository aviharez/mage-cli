kind: Service
apiVersion: v1
metadata:
  name: mage
  labels:
    app: mage
    app.kubernetes.io/component: mage
    app.kubernetes.io/instance: mage
    app.kubernetes.io/name: mage
    app.kubernetes.io/part-of: framework
  annotations:
    openshift.io/generated-by: OpenShiftWebConsole
spec:
  ports:
    - name: 8080-tcp
      protocol: TCP
      port: 8080
      targetPort: 8080
    - name: 8443-tcp
      protocol: TCP
      port: 8443
      targetPort: 8443
  selector:
    app: mage
