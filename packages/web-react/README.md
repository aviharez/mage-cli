# <picture><source media="(prefers-color-scheme: dark)" srcset="https://github.com/btriapitsyn/mage/raw/HEAD/docs/references/badges/mage-logo-dark.svg"><img src="https://github.com/btriapitsyn/mage/raw/HEAD/docs/references/badges/mage-logo-light.svg" width="32" height="32" align="absmiddle" /></picture> @mage/web

[![GitHub stars](https://img.shields.io/github/stars/btriapitsyn/mage?style=flat&logo=data%3Aimage%2Fsvg%2Bxml%3Bbase64%%%3D&logoColor=FFFCF0&labelColor=100F0F&color=66800B)](https://github.com/btriapitsyn/mage/stargazers)
[![GitHub release](https://img.shields.io/github/v/release/btriapitsyn/mage?style=flat&logo=data%3Aimage%2Fsvg%2Bxml%3Bbase64%%%3D&logoColor=FFFCF0&labelColor=100F0F&color=205EA6)](https://github.com/btriapitsyn/mage/releases/latest)
[![Discord](https://img.shields.io/badge/Discord-join.svg?style=flat&labelColor=100F0F&color=8B7EC8&logo=discord&logoColor=FFFCF0)](https://discord.gg/ZYRSdnwwKA)

Run [Mage](https://mage.ai) in your browser. Install the CLI, open `localhost:3000`, done. Works on desktop browsers, tablets, and phones as a PWA.

Full project overview, screenshots, and all features: [github.com/btriapitsyn/mage](https://github.com/btriapitsyn/mage)

## Install

```bash
curl -fsSL https://raw.githubusercontent.com/btriapitsyn/mage/main/scripts/install.sh | bash
```

Or install manually: `bun add -g @mage/web` (or npm, pnpm, yarn).

> **Prerequisites:** [Mage CLI](https://mage.ai) installed, Node.js 22+.

## Usage

```bash
mage                          # Start on port 3000
mage --port 8080              # Custom port
mage --lan --port 3000        # Listen on LAN (0.0.0.0)
mage --ui-password secret     # Password-protect UI
mage startup enable           # Start at login as a native service
MAGE_UI_PASSWORD=secret mage startup enable # Save service password env
mage startup status           # Show startup service status
mage startup disable          # Remove startup service
mage tunnel help              # Tunnel lifecycle commands
mage tunnel providers         # Show provider capabilities
mage tunnel profile add --provider cloudflare --mode managed-remote --name prod-main --hostname app.example.com --token <token>
mage tunnel start --profile prod-main
mage tunnel start --provider cloudflare --mode quick --qr
mage tunnel start --provider cloudflare --mode managed-local --config ~/.cloudflared/config.yml
mage tunnel status --all      # Show tunnel state across instances
mage tunnel stop --port 3000  # Stop tunnel only (server stays running)
mage connect-url --port 3000  # Add this server to Mage Desktop
mage connect-url --server http://host:3000 --qr
mage connect-url --port 3000 --qr
mage logs                     # Follow latest instance logs
MAGE_PORT=4096 MAGE_SKIP_START=true mage                    # Connect to external Mage server
MAGE_HOST=https://myhost:4096 MAGE_SKIP_START=true mage  # Connect via custom host/HTTPS
mage stop                     # Stop server
mage update                   # Update to latest version
```

`startup enable` snapshots your current environment into the native service so startup behaves like you launched `mage` from the same shell. This preserves provider tokens, PATH, SSH agent settings, and other CLI auth/config env vars. Use `--no-env-snapshot` for a minimal service env.

### Tunnel behavior notes

- One active tunnel per running Mage instance (port).
- Starting a different tunnel mode/provider on the same instance replaces the active tunnel.
- Replacing or stopping a tunnel revokes existing connect links and invalidates remote tunnel sessions.
- Connect links are one-time tokens; generating a new link revokes the previous unused link.

### Connect other Mage apps

Use `connect-url` when a web/API server should be added to Mage Desktop or another Mage app. If no server is running on the selected port, Mage starts one first.

```bash
mage connect-url --port 3000
mage connect-url --port 3000 --qr
mage connect-url --port 3000 --json
mage connect-url --port 3000 --name "Workstation"
mage connect-url --port 3000 --lan --server http://workstation.local:3000 --qr
```

### Headless/API-only server for Desktop

Use this on a remote machine when you want Mage running as a web/API server, then connect to it from Mage Desktop on another machine:

```bash
mage connect-url --port 3000 --api-only --lan --server http://workstation.local:3000 --qr --ui-password your-password
```

`--api-only` starts API routes without serving browser UI assets. `--lan` binds the server so other machines can reach it. `--server` is the address saved into the Desktop connection link. `--ui-password` protects browser access if UI routes are enabled elsewhere; the generated client token is what Desktop uses for API access.

This creates a remote client token and prints an `mage://connect?...` link. The link contains the server URL, token, label, and payload version. In Mage Desktop, paste it in **Settings -> Remote Instances -> Direct Instances -> Import Link** to add that server as an Instance.

If the server was started with `--lan` or `--host 0.0.0.0`, `connect-url` automatically advertises a detected LAN IP instead of `127.0.0.1`. Use `--server <url>` when you want to advertise a specific DNS name, Tailscale address, reverse proxy URL, or HTTPS endpoint.

If you are exposing the server beyond localhost, start it with a password:

```bash
mage serve --lan --port 3000 --ui-password your-password
```

Generating a client token does not automatically password-protect the hosted browser UI. `--ui-password` protects browser access; the client token lets another Mage app connect to this server.

<details>
<summary>Connect to external Mage server</summary>

```bash
MAGE_PORT=4096 MAGE_SKIP_START=true mage
MAGE_HOST=https://myhost:4096 MAGE_SKIP_START=true mage
```

| Variable | Description |
|----------|-------------|
| `MAGE_HOST` | Full base URL of external server (overrides `MAGE_PORT`) |
| `MAGE_PORT` | Port of external server |
| `MAGE_SKIP_START` | Skip starting embedded Mage server |
| `MAGE_MAGE_HOSTNAME` | Bind hostname for managed Mage server (default: `127.0.0.1`, use `0.0.0.0` for LAN/remote access — trusted networks only) |
| `MAGE_HOST` | Bind hostname for the Mage web server (default: `127.0.0.1`; use `0.0.0.0` for LAN/remote access — trusted networks only) |
| `MAGE_VERBOSE_REQUEST_LOGS` | Set to `true` to log every HTTP request; disabled by default to keep user logs small |
| `MAGE_SKIP_API_COMPRESSION` | Set to `true` to disable gzip compression for `/api/*` responses |
| `MAGE_COMPRESS_API` | Set to `true` to force `/api/*` compression, or `false` to disable it. Desktop runtime disables API compression by default to reduce local sidecar CPU use |

</details>

<details>
<summary>Bind managed Mage to LAN / Tailscale</summary>

```bash
MAGE_MAGE_HOSTNAME=0.0.0.0 mage --port 3000
```

**Security note:** binding to `0.0.0.0` exposes the server on all network interfaces — use only on trusted networks and protect with firewall rules or `--ui-password`.

</details>

**Optional env vars:**
```yaml
environment:
  UI_PASSWORD: your_secure_password
  MAGE_TUNNEL_MODE: quick # quick | managed-remote | managed-local
  MAGE_TUNNEL_PROVIDER: cloudflare
```

For `managed-remote` mode, also set:

```yaml
environment:
  MAGE_TUNNEL_MODE: managed-remote
  MAGE_TUNNEL_HOSTNAME: app.example.com
  MAGE_TUNNEL_TOKEN: <token>
```

For `managed-local` mode, you can set:

```yaml
environment:
  MAGE_TUNNEL_MODE: managed-local
  MAGE_TUNNEL_CONFIG: /home/mage/.cloudflared/config.yml
```

Managed-local path note: `MAGE_TUNNEL_CONFIG` must use a container path under `/home/mage/...`. If the config file references `credentials-file`, ensure that JSON path is also mounted and reachable inside the container.

**Data directory:** mount `data/` for persistent storage. Ensure permissions:
```bash
mkdir -p data/mage data/mage/share data/mage/config data/ssh
chown -R 1000:1000 data/
```

</details>

<details>
<summary>Background & daemon mode</summary>

```bash
mage             # Runs in background by default
mage stop        # Stop background server
```

</details>

<details>
<summary>systemd service (VPN / LAN access)</summary>

Use `--foreground` to keep the CLI process alive so systemd (or any other process manager) can track and restart it. Combine with `MAGE_HOST` to connect to an Mage instance running as a separate service.

**`~/.config/systemd/user/mage.service`**
```ini
[Unit]
Description=Mage Server

[Service]
Type=simple
ExecStart=mage serve --port 4095
Environment="PATH=/home/linuxbrew/.linuxbrew/bin:/home/linuxbrew/.linuxbrew/sbin:/home/YOU/.local/bin:/home/YOU/.npm-global/bin:/usr/local/bin:/usr/bin:/bin"
Environment=SSH_AUTH_SOCK=%t/ssh-agent.socket
Restart=on-failure
RestartSec=5

[Install]
WantedBy=default.target
```

> **Why set `PATH` and `SSH_AUTH_SOCK`?**
> systemd user services start with a minimal environment — no shell profile is sourced.
> Without an explicit `PATH`, Mage won't find tools installed via Homebrew, npm, or `~/.local/bin`.
> Without `SSH_AUTH_SOCK`, git operations over SSH (push, pull, clone) will fail.
> `%t` expands to `$XDG_RUNTIME_DIR` (e.g. `/run/user/1000`), where most SSH agents write their socket.

**`~/.config/systemd/user/mage.service`**
```ini
[Unit]
Description=Mage Web Server
After=mage.service

[Service]
Type=simple
ExecStart=mage serve --port 3000 --host 0.0.0.0 --ui-password your-password --foreground
Environment="MAGE_HOST=http://localhost:4095"
Environment="MAGE_SKIP_START=true"
Restart=on-failure
RestartSec=5

[Install]
WantedBy=default.target
```

```bash
systemctl --user daemon-reload
systemctl --user enable --now mage mage
```

`--host 0.0.0.0` is required to listen on all interfaces (the default is `127.0.0.1`). Use `--host <ip>` or `MAGE_HOST=<ip>` to bind to a specific interface instead.

</details>

## What makes the web version special

- **Remote access** - Cloudflare tunnel with QR onboarding. Scan from your phone, start coding.
- **Mobile-first PWA** - optimized chat controls, keyboard-safe layouts, drag-to-reorder projects
- **Background notifications** - know when your agent finishes, even from another tab
- **Self-update** - update and restart from the UI, server settings stay intact
- **Cross-tab tracking** - session activity stays in sync across browser tabs

- Cloudflare tunnel access with quick, managed-remote, and managed-local modes
- One-scan onboarding with tunnel QR + password URL helpers
- Mobile-first experience: optimized chat controls, keyboard-safe layouts, and attachment-friendly UI
- Background notifications plus reliable cross-tab session activity tracking
- Built-in self-update + restart flow that keeps your server settings intact

## License

MIT
